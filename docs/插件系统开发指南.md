# URLDB 插件系统开发指南

## 📋 目录

1. [插件系统概述](#插件系统概述)
2. [插件类型](#插件类型)
   - [钩子插件](#1-钩子插件-hook-plugins)
   - [压缩包插件](#2-压缩包插件-package-plugins)
   - [数据库迁移功能](#3-数据库迁移-migrate-功能)
3. [插件目录结构](#插件目录结构)
4. [插件开发基础](#插件开发基础)
5. [系统事件与钩子](#系统事件与钩子)
6. [事件对象详情](#事件对象详情)
7. [可用的API与绑定](#可用的api与绑定)
8. [插件配置](#插件配置)
9. [路由与接口开发](#路由与接口开发)
10. [定时任务](#定时任务)
11. [文件操作](#文件操作)
12. [数据库操作](#数据库操作)
13. [安全与验证](#安全与验证)
14. [错误处理与日志](#错误处理与日志)
15. [调试技巧](#调试技巧)
16. [部署与管理](#部署与管理)

---

## 插件系统概述

URLDB插件系统是一个基于JavaScript/Goja的可扩展架构，允许开发者扩展系统功能。插件通过事件钩子、自定义路由、定时任务等方式与主系统集成。

### 核心特性
- **事件驱动**: 通过系统事件钩子响应系统变化
- **热重载**: 支持插件文件修改后自动重载
- **沙箱执行**: JavaScript代码在安全沙箱中执行
- **类型提示**: 支持TypeScript类型提示

### 插件系统架构
- **插件目录**: `plugin-system/hooks/`
- **类型定义**: `plugin-system/types/`
- **迁移文件**: `migrations/`

---

## 插件类型

### 1. 钩子插件 (Hook Plugins)

钩子插件是以 `.plugin.js` 结尾的JavaScript文件，直接放置在 `plugin-system/hooks/` 目录下。

#### 创建钩子插件
```javascript
/// <reference path="../pb_data/types.d.ts" />

/**
 * 插件名称: my_plugin
 * 显示名称: 我的插件
 * 作者: 开发者姓名
 * 版本: 1.0.0
 * 描述: 插件功能描述
 *
 * @config
 * @field {string} webhook_url Webhook URL "Webhook通知地址" @default "https://example.com/webhook"
 * @field {boolean} enable_notification 启用通知 "是否启用通知功能" @default true
 * @config
 */
```

### 2. 压缩包插件 (Package Plugins)

压缩包插件是完整的插件包格式，适用于复杂的插件项目，支持数据库迁移功能。

#### 创建压缩包插件
压缩包插件需要包含以下文件结构：
```
my_plugin.zip
├── package.json              # 插件配置文件
├── index.js                  # 主入口文件 (可选)
├── hooks/                    # 钩子文件目录
│   ├── my_hook.plugin.js
│   └── another_hook.plugin.js
└── migrate/                  # 数据库迁移目录 (可选)
    ├── install.sql          # 安装时执行的SQL
    └── uninstall.sql        # 卸载时执行的SQL
```

#### package.json 配置
```json
{
  "name": "my_complex_plugin",
  "version": "1.0.0",
  "description": "一个复杂的插件示例",
  "author": "开发者姓名",
  "main": "index.js",
  "hooks": ["my_hook.plugin.js"],
  "dependencies": [],
  "config": {
    "webhook_url": {
      "type": "string",
      "label": "Webhook URL",
      "default": "https://hooks.slack.com/...",
      "description": "通知发送的Webhook地址"
    },
    "enable_notifications": {
      "type": "boolean",
      "label": "启用通知",
      "default": true,
      "description": "是否启用通知功能"
    },
    "retry_count": {
      "type": "number",
      "label": "重试次数",
      "default": 3,
      "min": 1,
      "max": 10,
      "description": "失败时重试次数"
    }
  },
  "permissions": [
    "network",
    "filesystem"
  ]
}
```

### 3. 数据库迁移 (Migrate) 功能

**Hook 类型插件（单文件 .plugin.js）不支持 migrate 功能，只有压缩包插件支持通过 SQL 文件进行数据库结构迁移。**

#### migrate 规则说明：

- **❌ Hook 插件 (单文件 .plugin.js)**:
  - 不支持 migrate 功能
  - 无法使用 migrate() 函数或 SQL 迁移文件
  - 适合简单的逻辑处理插件

- **✅ 压缩包插件**:
  - 支持 SQL 迁移文件 (install.sql, uninstall.sql)
  - 自动执行安装/卸载迁移
  - 适合需要数据库结构的复杂插件

#### migrate 目录结构：
```
my_migration_plugin.zip
├── package.json              # 插件配置文件
├── index.js                  # 主入口文件
├── migrate/                  # 迁移目录
│   ├── install.sql          # 安装时执行的SQL
│   └── uninstall.sql        # 卸载时执行的SQL
├── hooks/
│   └── my_hook.plugin.js
└── README.md
```

#### SQL 文件规范：

**install.sql**:
- 插件安装时自动执行
- 创建表、索引等结构
- 插入初始数据
- 执行失败会回滚安装

**uninstall.sql**:
- 插件卸载时自动执行
- 删除相关表结构
- 清理插件数据
- 确保系统干净卸载

#### install.sql 示例：
```sql
-- 创建插件数据表
CREATE TABLE IF NOT EXISTS my_plugin_data (
    id INTEGER PRIMARY KEY,
    plugin_name VARCHAR(100) NOT NULL,
    message TEXT,
    config_json TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 创建索引
CREATE INDEX IF NOT EXISTS idx_my_plugin_data_name
ON my_plugin_data(plugin_name);

-- 插入初始数据
INSERT INTO my_plugin_data (plugin_name, message, config_json)
VALUES ('my_plugin', '插件安装时创建的默认数据', '{"enabled": true, "version": "1.0.0"}');
```

#### uninstall.sql 示例：
```sql
-- 删除索引
DROP INDEX IF EXISTS idx_my_plugin_data_name;

-- 删除插件数据表
DROP TABLE IF EXISTS my_plugin_data;
```

#### 最佳实践：

1. **命名规范**:
   - 表名使用插件前缀：plugin_name_table
   - 索引名包含表名：idx_table_field
   - 避免与系统表名冲突

2. **安全考虑**:
   - 使用 IF EXISTS 避免错误
   - 不删除系统表或数据
   - 只操作插件相关数据

3. **性能优化**:
   - 合理创建索引
   - 避免大量数据插入
   - 使用批量操作

4. **测试建议**:
   - 在测试环境验证 SQL
   - 测试安装和卸载流程
   - 验证数据完整性

---

## 插件目录结构

```
urldb/
├── plugin-system/
│   ├── hooks/                 # 插件文件存放目录
│   │   ├── my_plugin.plugin.js
│   │   └── config_demo.plugin.js
│   └── types/                 # TypeScript类型定义
│       └── types.d.ts
├── migrations/               # 数据库迁移文件（系统级）
└── main.go                  # 主程序入口
```

---

## 插件开发基础

### 插件文件命名

- **钩子插件**: 使用 `.plugin.js` 后缀
- **示例**: `my_plugin.plugin.js`

### 插件头部注释

```javascript
/// <reference path="../pb_data/types.d.ts" />

/**
 * my_plugin 钩子
 * 创建时间: 2026-01-05 10:00:00
 *
 * @name my_plugin
 * @display_name 我的插件
 * @author URLDB开发团队
 * @description 插件功能描述
 * @version 1.0.0
 * @category demo
 * @license MIT
 *
 * @config
 * @field {string} webhook_url Webhook URL "通知发送的Webhook地址" @default "https://hooks.example.com"
 * @field {boolean} enable_notification 启用通知 "是否启用通知功能" @default true
 * @config
 */
```

---

## 系统事件与钩子

### 事件类型

插件可以通过事件钩子响应系统事件：

#### 1. URL相关事件

```javascript
// URL添加事件
onURLAdd(function(event) {
    console.log("URL添加:", event.url.id, event.url.title);
    // 处理逻辑
});

// URL访问事件
onURLAccess(function(event) {
    console.log("URL访问:", event.url.id);
    // 处理逻辑
});

// URL更新事件
onURLUpdate(function(event) {
    console.log("URL更新:", event.url.id);
    // 处理逻辑
});

// URL删除事件
onURLDelete(function(event) {
    console.log("URL删除:", event.url.id);
    // 处理逻辑
});
```

#### 2. 用户相关事件

```javascript
// 用户登录事件
onUserLogin(function(event) {
    console.log("用户登录:", event.user.username);
    // 处理逻辑
});

// 用户登出事件
onUserLogout(function(event) {
    console.log("用户登出:", event.user.username);
    // 处理逻辑
});

// 用户注册事件
onUserRegister(function(event) {
    console.log("用户注册:", event.user.username);
    // 处理逻辑
});
```

#### 3. 分类与标签事件

```javascript
// 分类创建事件
onCategoryCreate(function(event) {
    console.log("分类创建:", event.category.name);
    // 处理逻辑
});

// 标签添加事件
onTagAdd(function(event) {
    console.log("标签添加:", event.tag.name);
    // 处理逻辑
});
```

#### 4. API相关事件

```javascript
// API请求事件
onAPIRequest(function(event) {
    console.log("API请求:", event.method, event.path);
    // 处理逻辑
});

// API响应事件
onAPIResponse(function(event) {
    console.log("API响应:", event.method, event.path, event.status);
    // 处理逻辑
});
```

#### 5. 待处理资源事件

```javascript
// 待处理资源添加事件
onReadyResourceAdd(function(event) {
    console.log("待处理资源添加:", event.ready_resource.url);
    // event.data 包含额外信息，如 is_filtered, filter_reason 等
    if (event.data.is_filtered) {
        console.log("资源被过滤:", event.data.filter_reason);
    }
    // 处理逻辑
});
```

---

## 事件对象详情

### onURLAdd 事件对象

```javascript
{
    url: {
        id: 156696,
        title: "URL标题",
        url: "https://example.com",
        description: "描述",
        category_id: 1,
        tags: ["标签1", "标签2"],
        is_valid: true,
        is_public: true,
        view_count: 0,
        created_at: "2025-12-29T23:49:04.556Z"
    },
    app: {
        name: "URLDB",
        version: "1.0.0"
    }
}
```

### onUserLogin 事件对象

```javascript
{
    user: {
        id: 1,
        username: "admin",
        email: "admin@example.com",
        role: "admin",
        is_active: true,
        last_login: "2025-12-29T23:49:04.556Z",
        created_at: "2025-12-29T23:49:04.556Z"
    },
    data: {
        ip: "127.0.0.1",
        user_agent: "浏览器信息",
        login_time: "2025-12-29T23:49:04.556Z"
    },
    app: {
        name: "URLDB",
        version: "1.0.0"
    }
}
```

---

## 可用的API与绑定

### 1. 数据库操作 (db)

```javascript
// 查询资源 - 使用对象形式的查询条件
const resources = db.find("resources", { category: "tech" });

// 获取记录数量
const count = db.count("resources", { category: "tech" });

// 执行原始SQL - 支持参数化查询
const result = db.raw("SELECT id, title, url FROM resources WHERE category = $1", ["tech"]);

// 保存记录 - 数据应为对象
const newResource = {
    title: "新资源",
    url: "http://example.com",
    category: "tech",
    description: "这是一个新资源"
};
const saved = db.save("resources", newResource);

// 更新记录
const updated = db.update("resources", saved.id, {
    title: "更新后的标题"
});

// 删除记录
const deleted = db.delete("resources", resourceId);

// 复杂查询示例
const complexResult = db.raw(`
    SELECT r.id, r.title, r.url, c.name as category_name
    FROM resources r
    LEFT JOIN categories c ON r.category_id = c.id
    WHERE r.category = $1
    ORDER BY r.created_at DESC
    LIMIT $2
`, ["technology", 10]);
```

### 2. 安全函数 (security)

```javascript
// 哈希运算
const md5Hash = $security.md5("string");
const sha256Hash = $security.sha256("string");
const sha512Hash = $security.sha512("string");

// 随机字符串生成
const randomStr = $security.randomString(16);

// JWT操作
const token = $security.createJWT(
    { user_id: 123, exp: Date.now() + 3600000 }, // 1小时过期
    "secret_key",
    3600
);
const payload = $security.parseJWT(token, "secret_key");

// 加密解密
const encrypted = $security.encrypt("Hello World", "key");
const decrypted = $security.decrypt(encrypted, "key");
```

### 3. 操作系统函数 (os)

```javascript
// 环境变量
const path = $os.getenv("PATH");
const home = $os.getenv("HOME");

// 临时目录
const tempDir = $os.tempDir();

// 平台信息
const platform = $os.platform();
const args = $os.args();
```

### 4. 文件系统操作 (filesystem)

```javascript
// 读取文件
const content = $filesystem.readFile("/path/to/file.txt");

// 写入文件
$filesystem.writeFile("/path/to/file.txt", "Hello World");

// 文件存在检查
const exists = $filesystem.fileExists("/path/to/file.txt");

// 文件大小
const size = $filesystem.fileSize("/path/to/file.txt");
```

### 5. 文件路径操作 (filepath)

```javascript
// 路径操作
const joined = $filepath.join("path", "to", "file.txt");
const base = $filepath.base("/path/to/file.txt");
const dir = $filepath.dir("/path/to/file.txt");
const ext = $filepath.ext("/path/to/file.txt");
const isAbs = $filepath.isAbs("/path/to/file.txt");
const clean = $filepath.clean("path//to///./file.txt");
```

### 6. 表单验证 (forms)

```javascript
const data = {
    name: "用户",
    email: "user@example.com",
    age: 25
};

const rules = {
    name: { required: true, minLength: 2, maxLength: 50 },
    email: { required: true, isEmail: true },
    age: { required: true, min: 18, max: 120 }
};

const result = $forms.validate(data, rules);
if (result.valid) {
    // 验证通过
} else {
    // 验证失败，result.errors 包含错误信息
}
```

### 7. HTTP客户端 (http)

```javascript
// GET请求
const getResponse = $http.get("https://api.example.com/data");

// POST请求
const postResponse = $http.post("https://api.example.com/data", {
    key: "value"
}, {
    headers: { "Content-Type": "application/json" }
});

// 自定义请求
const customResponse = $http.send("PUT", "https://api.example.com/data", {
    body: JSON.stringify({ key: "value" }),
    headers: { "Content-Type": "application/json" }
});
```

### 8. 邮件发送 (mails)

```javascript
// 发送邮件
const result = mails.send(
    "recipient@example.com",
    "邮件主题",
    "邮件内容",
    {
        from: "sender@example.com",
        cc: ["cc@example.com"],
        attachments: ["/path/to/attachment.pdf"]
    }
);
```

---

## 插件配置

### 配置声明

在插件头部注释中使用 `@config` 标签声明配置：

```javascript
/**
 * @config
 * @field {string} webhook_url Webhook URL "Webhook通知地址" @default "https://hooks.example.com"
 * @field {boolean} enable_notification 启用通知 "是否启用通知功能" @default true
 * @field {number} retry_count 重试次数 "失败重试次数" @default 3
 * @field {select} log_level 日志级别 ["debug", "info", "warn", "error"] @default "info"
 * @field {text} custom_message 自定义消息 "自定义通知消息" @optional @default "默认消息"
 * @config
 */
```

### 配置类型

1. **string**: 字符串输入
2. **number**: 数字输入
3. **boolean**: 布尔值（复选框）
4. **select**: 下拉选择
5. **text**: 多行文本
6. **password**: 密码输入

### 获取配置

```javascript
// 获取插件配置
const config = getPluginConfig("my_plugin");

if (config) {
    console.log("Webhook URL:", config.webhook_url);
    console.log("通知启用:", config.enable_notification);
    console.log("重试次数:", config.retry_count);
}
```

### 设置配置

```javascript
// 设置插件配置
setPluginConfig("my_plugin", {
    webhook_url: "https://new-webhook.example.com",
    enable_notification: true,
    retry_count: 5
});
```

---

## 路由与接口开发

### 添加自定义路由

```javascript
// GET路由
routerAdd("GET", "/api/my-endpoint", (e) => {
    return e.json(200, {
        message: "Hello from custom endpoint",
        timestamp: new Date().toISOString()
    });
});

// POST路由
routerAdd("POST", "/api/my-endpoint", (e) => {
    const body = e.body; // 获取请求体
    const headers = e.headers; // 获取请求头
    const query = e.query; // 获取查询参数

    return e.json(200, {
        message: "POST received",
        data: body
    });
});

// 带参数的路由
routerAdd("GET", "/api/my-endpoint/:id", (e) => {
    const id = e.params.id; // 获取路径参数

    return e.json(200, {
        message: "Item ID: " + id
    });
});
```

### 路由处理函数

路由处理函数接收一个事件对象 `e`，包含以下属性：

- `e.body`: 请求体（POST/PUT等）
- `e.headers`: 请求头
- `e.query`: 查询参数
- `e.params`: 路径参数
- `e.json(status, data)`: 返回JSON响应

---

## 定时任务

### 添加定时任务

```javascript
// 使用Cron表达式添加定时任务
// 每分钟执行: "*/1 * * * *"
// 每小时执行: "0 * * * *"
// 每天凌晨2点: "0 2 * * *"
// 每周日凌晨3点: "0 3 * * 0"

cronAdd("my_task", "*/5 * * * *", () => {
    console.log("定时任务执行:", new Date().toISOString());

    try {
        // 你的任务逻辑
        const config = getPluginConfig("my_plugin");

        if (config && config.enable_notification) {
            // 执行定期任务
            console.log("执行周期性检查...");
        }
    } catch (error) {
        console.error("定时任务执行失败:", error.message);
    }
});
```

### 移除定时任务

```javascript
// 移除定时任务
cronRemove("my_task");
```

---

## 文件操作

### 读写文件

```javascript
// 写入文件
const filePath = $os.tempDir() + "/my_file.txt";
$filesystem.writeFile(filePath, "Hello World");

// 读取文件
const content = $filesystem.readFile(filePath);
console.log("文件内容:", content);

// 检查文件是否存在
if ($filesystem.fileExists(filePath)) {
    console.log("文件存在");
}

// 获取文件大小
const size = $filesystem.fileSize(filePath);
console.log("文件大小:", size);
```

### 目录操作

```javascript
// 创建目录
const dirPath = $os.tempDir() + "/my_dir";
$os.createDir(dirPath);

// 列出目录内容
const files = $filesystem.readdir(dirPath);
console.log("目录内容:", files);
```

---

## 数据库操作

### 查询操作

```javascript
// 简单查询
const resources = db.find("resources", {});

// 条件查询
const techResources = db.find("resources", {
    category: "technology"
});

// 限制结果数量
const recentResources = db.find("resources", {}, {
    limit: 10,
    order: "created_at DESC"
});

// 计数查询
const total = db.count("resources", {
    category: "technology"
});
```

### 保存操作

```javascript
// 保存新记录
const newResource = {
    title: "新资源",
    url: "http://example.com",
    category: "technology",
    description: "这是一个示例资源"
};
const saved = db.save("resources", newResource);

// 更新记录
const updated = db.update("resources", saved.id, {
    title: "更新后的标题"
});
```

### 原始SQL查询

```javascript
// 执行原始SQL查询
const result = db.raw(`
    SELECT id, title, url
    FROM resources
    WHERE category = $1
    ORDER BY created_at DESC
    LIMIT $2
`, ["technology", 10]);

// 执行更新语句
const updateResult = db.raw(`
    UPDATE resources
    SET updated_at = NOW()
    WHERE id = $1
`, [resourceId]);
```

---

## 安全与验证

### 输入验证

```javascript
// 使用表单验证
const validationRules = {
    email: { required: true, isEmail: true },
    username: {
        required: true,
        minLength: 3,
        maxLength: 20,
        pattern: "^[a-zA-Z0-9_]+$" // 只允许字母、数字、下划线
    },
    age: { required: true, min: 18, max: 120 }
};

const data = {
    email: "user@example.com",
    username: "testuser",
    age: 25
};

const validationResult = $forms.validate(data, validationRules);

if (validationResult.valid) {
    console.log("验证通过");
} else {
    console.log("验证失败:", validationResult.errors);
}
```

### 安全哈希

```javascript
// 生成安全哈希
const passwordHash = $security.sha256(password + salt);
const fileHash = $security.md5(fileContent);

// 生成随机字符串
const token = $security.randomString(32);
const apiKey = $security.randomString(64);
```

### JWT令牌

```javascript
// 创建JWT令牌
const token = $security.createJWT(
    {
        user_id: 123,
        username: "testuser",
        exp: Math.floor(Date.now() / 1000) + 3600 // 1小时后过期
    },
    "your-secret-key",
    3600 // 过期时间（秒）
);

// 验证JWT令牌
try {
    const payload = $security.parseJWT(token, "your-secret-key");
    console.log("用户ID:", payload.user_id);
} catch (error) {
    console.error("令牌验证失败:", error.message);
}
```

---

## 错误处理与日志

### 错误处理

```javascript
// 使用try-catch处理错误
try {
    const result = someRiskyOperation();
    console.log("操作成功:", result);
} catch (error) {
    console.error("操作失败:", error.message);
    // 记录详细错误信息
    console.error("错误堆栈:", error.stack);
}
```

### 日志记录

```javascript
// 使用日志函数记录信息
log("debug", "调试信息");
log("info", "一般信息");
log("warn", "警告信息");
log("error", "错误信息");

// 带插件名称的日志
log("info", "插件消息内容", "plugin_name");

// 记录插件特定信息
const config = getPluginConfig("my_plugin");
if (config && config.log_level === "debug") {
    log("debug", "详细调试信息");
}
```

---

## 部署与管理

### 插件部署

1. **文件位置**: 将插件文件放置在 `plugin-system/hooks/` 目录下
2. **文件格式**: 确保文件名以 `.plugin.js` 结尾
3. **重启系统**: 插件会在系统启动时自动加载
4. **热重载**: 修改插件文件后，系统会自动重新加载

### 插件启用/禁用

```javascript
// 通过API接口管理插件状态
// 通过HTTP请求或管理界面控制插件启用状态
```

### 插件管理API

系统提供以下API用于插件管理：

- `GET /api/plugins`: 获取所有插件列表
- `GET /api/plugins/:name`: 获取特定插件信息
- `POST /api/plugins/:name/enable`: 启用插件
- `POST /api/plugins/:name/disable`: 禁用插件
- `PUT /api/plugins/:name/config`: 更新插件配置

### 最佳实践

1. **错误处理**: 始终使用try-catch包装可能出错的代码
2. **日志记录**: 记录重要操作和错误信息
3. **性能优化**: 避免在高频事件中执行耗时操作
4. **安全性**: 验证所有外部输入，避免注入攻击
5. **资源管理**: 及时清理临时文件和资源

## 调试技巧

### 调试方法

1. **使用日志函数**:
   ```javascript
   log("debug", "调试信息", "my_plugin");
   log("info", "一般信息", "my_plugin");
   log("warn", "警告信息", "my_plugin");
   log("error", "错误信息", "my_plugin");
   ```

2. **查看实时日志**:
   - 在插件管理界面查看实时日志输出
   - 日志会保存到数据库，可通过管理界面查看
   - 支持按级别过滤日志

3. **插件热重载**:
   - 插件支持热重载，修改后自动生效
   - 无需重启系统即可测试插件修改
   - 有助于快速迭代开发

4. **测试环境验证**:
   - 先在测试环境验证插件功能
   - 确保插件在生产环境中表现符合预期
   - 验证错误处理和边界情况

5. **压缩包插件调试**:
   - 可以解压查看文件结构进行调试
   - 检查配置文件的正确性
   - 验证SQL语句的语法

## 附录

### Cron表达式语法

| 字段 | 取值 | 说明 |
|------|------|------|
| 分钟 | 0-59 | |
| 小时 | 0-23 | |
| 日期 | 1-31 | |
| 月份 | 1-12 | |
| 星期 | 0-7 | 0和7都表示周日 |

**示例**:
- `*/5 * * * *` - 每5分钟执行
- `0 2 * * *` - 每天凌晨2点执行
- `0 0 * * 0` - 每周日凌晨执行

### 常见问题

1. **插件不加载**: 检查文件名是否以`.plugin.js`结尾
2. **API路由冲突**: 确保路由路径唯一
3. **性能问题**: 避免在事件钩子中执行耗时操作
4. **配置不生效**: 检查配置声明格式是否正确

### 安全建议

1. **输入验证**: 始终验证用户输入
2. **SQL注入防护**: 使用参数化查询
3. **文件路径验证**: 避免路径遍历攻击
4. **API访问控制**: 实施适当的访问控制